{
  "name": "grateful-gamma",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "touch dist/.nojekyll && gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "astro": "^5.12.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}

{
  "name": "grateful-gamma",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && echo 'sbecagol.com' > ./dist/CNAME && gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.1",
    "astro": "^5.12.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}

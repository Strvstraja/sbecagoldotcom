// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://sbecagol.github.io',
  base: '/grateful-gamma',
  build: {
    assets: 'assets'
  }
});
